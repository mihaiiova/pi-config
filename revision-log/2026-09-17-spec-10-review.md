# Review: spec-review of #10 (project state + append-only session history)
**Date:** 2026-09-17
**Session:** Review of `spec/10-add-project-state-and-append-only-session-history` — `.pi/state.json` resume projection, write-once `.pi/sessions/*.json` records, `/session-status|history|cost` commands, and version/config provenance.

## History Checked
- `2026-09-17-spec-9-review.md`
- `2026-09-16-spec-8-review.md`
- `2026-09-16-spec-7-review.md`
- `2026-09-16-spec-6-review.md`

## Recurring Patterns
- None found. The spec-7/spec-8 inline fan-out truncation pattern did **not** recur this session: both review axes were launched with file-backed `output` paths and the full reports were read directly from disk.

## Scores
| Dimension | Score |
|-----------|-------|
| Friction | 0.3 |
| Repetition | 0.2 |
| Missing capability | 0.2 |
| Knowledge gap | 0.2 |
| Fragility | 0.2 |

## Suggestions
No significant improvement opportunities found. (No dimension crossed 0.6.)

## Changes Made
- None (no accepted suggestions)

## Notes
- Two-axis code review ran via two read-only `reviewer` sub-agents (standards + spec), launched through a `workflowScript` fan-out with explicit per-child `output` files. Full reports were read from `standards-report.md` / `spec-report.md` — no truncation recovery needed, closing out the recurring problem logged for spec-7 and spec-8.
- Reviewers are read-only (no `bash`), so the Spec reviewer could not run `tests/project-state/test.sh` itself; the parent ran all five area runners through the shared `run-checks.sh` (5/5 PASS) both before and after the in-scope fixes.
- Two in-scope blockers were fixed and re-verified: (1) lifecycle-boundary writes now re-derive branch/spec/phase from git + GitHub labels instead of replaying the session-start snapshot (extracted pure `reconcile.mjs` helpers, unit-tested); (2) finalized records now capture reliably reported per-agent subagent usage via `collectSubagentUsage` reading `subagent-artifacts/*_meta.json` (the same shape `/spec-cost` treats as reliable).
- Residual non-blocking notes: `pendingWork`/`checkResults` fields stay null until a reliable source exists (spec-mandated, "when reliably known"); the setup path for other projects' `.gitignore` rules is documented in the extension README rather than auto-edited; `getHeader()` is unused; a future-schema `state.json` is treated as missing and re-created rather than rejected.

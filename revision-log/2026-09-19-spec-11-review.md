# Review: spec-review of #11 (enrich session records with workflow and delegation identity)
**Date:** 2026-09-19
**Session:** Review of `spec/11-enrich-session-identity` — `workflow` identity from user `/spec-*` commands, per-agent `status`/`model` from `*_meta.json`, `checkResults` from `run-checks.sh` `results.json`, header `startedAt`.

## History Checked
- `2026-09-17-spec-10-review.md`
- `2026-09-17-spec-9-review.md`
- `2026-09-16-spec-8-review.md`
- `2026-09-16-spec-7-review.md`
- `2026-09-16-spec-6-review.md`

## Recurring Patterns
- None found. The spec-7/spec-8 inline fan-out truncation was already resolved in spec-10 with file-backed `output` paths; this session used the same file-backed fan-out and read both reports directly from disk.

## Scores
| Dimension | Score |
|-----------|-------|
| Friction | 0.3 |
| Repetition | 0.2 |
| Missing capability | 0.2 |
| Knowledge gap | 0.2 |
| Fragility | 0.3 |

## Suggestions
No significant improvement opportunities found. (No dimension crossed 0.6.)

## Changes Made
- None (no accepted suggestions)

## Notes
- Two-axis code review ran via two read-only `reviewer` sub-agents (standards + spec), launched through a `workflowScriptPath` fan-out with explicit per-child `output` files; full reports read directly from disk.
- Verification via the shared `run-checks.sh` runner: `project-state`, `run-checks`, and `spec-orchestration` — 3/3 PASS before and after the in-scope fixes. (First attempt failed on `tests/project-state/test.sh` with "Permission denied"; it is mode `100644`, so it must run as `bash tests/project-state/test.sh` — pre-existing, unrelated to this spec.)
- Five in-scope fixes applied and re-verified, all surfaced by the reviewers: (1) `detectWorkflow` regex was unanchored, so prose/paths like `docs/spec-lifecycle.md` or `.pi/artifacts/spec-review/…` produced a false `workflow` — now anchored to the `/` command form, with prose/path regression tests; (2) `statusOf` returned `null` for `[non-zero, missing]` despite the spec's "failed when any is non-zero" — a known failure now wins, with a mixed-case test; (3) `readCheckResults` accepted any plain object — now validates values are `"passed"`/`"failed"`; (4) `checkResults` type contract reconciled across `metadata.d.mts` and `schema.d.mts`; (5) root `README.md` `/session-history` one-liner updated to mention workflow + delegation roles.
- Accepted as judgement calls (not fixed): the spec's `access` (read-only vs write) render map — no user story or test requires it, and no deterministic role→access mapping exists in the repo, so a "fixed map" would fabricate data ("null beats wrong"); a Data Clumps note in `collectSubagentUsage` (the `statusOf`/`modelOf` helpers are cleaner kept separate); and the grep-based assertions in `tests/project-state/test.sh`, which are the repo's documented convention for Pi-coupled surfaces.
